#ifndef _FUNCTIONS_
#define _FUNCTIONS_

#include <iostream>
#include <vector>

#include <random>

#include "classes.h"

void runSimulate(int &allR, int &allW, int &completeR, int &completeW);
void showStat(int &allR, int &allW, int &completeR, int &completeW);

#endif /* _FUNCTIONS_ */
